[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pyrtipc"
version = "0.0.0"
authors = [{name = "Simon Maurer", email = "mail@maurer.systems"}]
license = "Apache-2.0"
license-files = ["LICENSE"]
readme = "README.md"
dynamic = ["version", "description"]

[project.urls]
Home = "https://github.com/mausys/pyrtipc"

dependencies = []
